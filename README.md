# Pop Collection Backend

Node.js with Express backend that passes pops when the api endpoint is requested.

## Built With

- Node.js
- Express

## Installation

- Run `npm install` to install dependencies
- Run `npm start` to start the application
